class: Flashlist
table: flashlist
fields:
    uuid: String
    title: String
    color: String
    items: List<FlashlistItem?>?, !persist
    authors: List<AppUser?>?, !persist
    isCollapsed: bool?, !persist
    createdAt: DateTime
    updatedAt: DateTime?
